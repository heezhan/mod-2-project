

<%= form_for @review do |f| %>
<%= f.label :content %>
<%= f.text_area :content %>
<%= f.label :stars %>
<%= f.number_field :stars %>
<%= f.collection_select :restaurant_id, Restaurant.all, :id, :name %>

<%= f.submit "Submit" %>

<% end %>
